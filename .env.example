# Salesforce OAuth
SF_CLIENT_ID=your_connected_app_consumer_key
SF_CLIENT_SECRET=your_connected_app_consumer_secret
SF_REDIRECT_URI=http://localhost:8400/callback
# Leave empty for production, set to "test" for sandbox
SF_DOMAIN=
# These are populated automatically after first OAuth login:
# SF_REFRESH_TOKEN (stored in .token_cache.json locally, GitHub Secret in CI)
# SF_INSTANCE_URL (stored in .token_cache.json locally, GitHub Secret in CI)

# Gmail API OAuth
GMAIL_SENDER=francis.phan@vinesofmendoza.com
GMAIL_CLIENT_ID=your_google_oauth_client_id
GMAIL_CLIENT_SECRET=your_google_oauth_client_secret
# GMAIL_REFRESH_TOKEN is stored in .gmail_token.json locally, GitHub Secret in CI
